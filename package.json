{
  "name": "food-delivery-app",
  "private": true,
  "version": "1.0.0",
  "description": "A full stack food delivery app with Nestjs and React-native",
  "main": "index.js",
  "scripts": {
    "dev:api": "pnpm --filter api run start:dev",
    "dev:mobile": "pnpm --filter mobile start",
    "dev": "concurrently \"pnpm dev:api\" \"pnpm dev:mobile\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "^11.17.0",
      "onFail": "download"
    }
  },
  "type": "module",
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}
